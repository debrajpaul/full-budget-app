{
  "name": "@services",
  "description": "Service layer for handling business logic",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write .",
    "test": "jest --config jest.config.js"
  },
  "dependencies": {
    "@auth": "workspace:*",
    "@common": "workspace:*",
    "@db": "workspace:*",
    "@parser": "workspace:*",
    "@logger": "workspace:*",
    "@client": "workspace:*",
    "bcryptjs": "^3.0.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0"
  }
}
