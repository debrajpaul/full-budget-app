{
  "name": "@common",
  "description": "Common service providing foundational functionality for the application",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@logger": "workspace:*",
    "graphql-yoga": "^5.13.4"
  }
}
