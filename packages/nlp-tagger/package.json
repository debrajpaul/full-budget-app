{
  "name": "@nlp-tagger",
  "description": "NLP Tagger service for processing and tagging text data",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write .",
    "test": "jest --config jest.config.js"
  }
}
