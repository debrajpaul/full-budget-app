{
  "name": "@client",
  "description": "Client for managing file uploads and downloads",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write .",
    "test": "jest --config jest.config.js"
  },
  "dependencies": {
    "@common": "workspace:*",
    "@logger": "workspace:*",
    "@aws-sdk/client-s3": "^3.300.0",
    "@aws-sdk/client-sqs": "^3.300.0",
    "@aws-sdk/client-comprehend": "^3.873.0"
  }
}
