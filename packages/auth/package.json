{
  "name": "@auth",
  "description": "Authentication service for managing user sessions and security",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write .",
    "test": "jest --config jest.config.js"
  },
  "dependencies": {
    "@common": "workspace:*",
    "@logger": "workspace:*",
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2"
  }
}
