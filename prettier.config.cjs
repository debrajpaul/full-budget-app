/** @type {import("prettier").Config} */
module.exports = {
  // Explicit defaults so editors and CI share the same formatter settings
  singleQuote: false,
  trailingComma: "es5",
  printWidth: 80,
  tabWidth: 2,
  semi: true,
};
