{
  "name": "@app/api",
  "description": "API service for handling requests",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --project ../../tsconfig.json --require tsconfig-paths/register --respawn src/index.ts"
  },
  "dependencies": {
    "@auth": "workspace:*",
    "@core": "workspace:*",
    "@db": "workspace:*",
    "graphql": "^16.11.0",
    "graphql-yoga": "^5.13.4"
  }
}
