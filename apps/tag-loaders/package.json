{
  "name": "@app/tag-loaders",
  "description": "Tag loaders for batching and caching requests",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outdir=dist --external:aws-sdk",
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write .",
    "dev": "ts-node-dev --respawn src/index.ts"
  },
  "dependencies": {
    "@auth": "workspace:*",
    "@aws-sdk/client-dynamodb": "^3.844.0",
    "@aws-sdk/client-s3": "^3.300.0",
    "@aws-sdk/client-sqs": "^3.300.0",
    "@aws-sdk/lib-dynamodb": "^3.844.0",
    "@client": "workspace:*",
    "@common": "workspace:*",
    "@db": "workspace:*",
    "@logger": "workspace:*",
    "@parser": "workspace:*",
    "@services": "workspace:*",
    "@nlp-tagger": "workspace:*",
    "aws-lambda": "^1.0.7",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152"
  }
}
